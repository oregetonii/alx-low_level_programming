#include "main.h"

/**
 * _strlen - returns the length of a string
 * @*s: pointer to the string wose length is returned
 * Return: 0
 */

int _strlen(char *s)
{
	char c;
	char *p = &c;;

	while (*p != '\0')
	
		p++;
	

	int len = p - p++;

	return (len);
}
