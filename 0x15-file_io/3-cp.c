#include "main.h"

/**
 * cp - copies the contents of the first file 
 * into the second file
 * @file_from: the file whose contents are copied
 * @file_to: the file to which the contents are written
 *
 * Return: nothing
 */
void cp(const char *file_from, const char *file_to) 
{
	/* open the first file */
	file = fopen(file_from, );
	/* copy its contents to a buffer */
	/* close it */

	/* open the second file */
	/* write the buffer contents to it */
	/* close it */
